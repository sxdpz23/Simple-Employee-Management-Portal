import { ETStatus } from "./etStatus"

export class EmployeeTraining {
    emailId!: string
    courseName!: string
    courseCode!: string
    score!: string
    hoursSpent!: number
    dateCompleted!: Date
    status!: ETStatus
}